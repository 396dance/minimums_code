<!--コメント一覧-->
<% outfit.outfit_comments.each do |outfit_comment| %>
  <div>
    <span><%= image_tag outfit_comment.user.get_profile_image(40,40) %></span>
      <span style="font-size: 8px"><%= outfit_comment.user.nickname %></span>
      <span><%= outfit_comment.comment %></span>
      <span style="font-size: 8px"><%= outfit_comment.created_at.strftime('%Y/%m/%d') %></span>

      <% if outfit_comment.user == current_user %>
        <%= link_to "削除", outfit_outfit_comment_path(outfit,outfit_comment), method: :delete, remote: true, data: {confirm: "本当に削除しますか？"}, class: "btn btn-sm btn-danger p-0 p-1", style: "font-size: 8px;" %>
      <% end %>
  </div>
<% end %>

