  <div class="search_form">
    <%= form_with url: search_path, local: true, method: :get do |f| %>
      <%= f.text_field :word, placeholder:"キーワード検索" %>
      <%= f.select :range, options_for_select([['ニックネーム'], ['投稿タイトル']]) %>
      <% if :word.nil? %>
        <%= 空です %>
      <% else %>
        <%= f.submit "検索", class: "btn btn-primary" %>
      <% end %>
    <% end %>
  </div>
