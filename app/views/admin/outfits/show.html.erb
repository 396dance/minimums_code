<h4>管理者側の服装詳細ページ</h4>

<p><%= image_tag @outfit.image %></p>

<p>タイトル</p>
<p><%= @outfit.title %></p>

<p>本文</p>
<p><%= @outfit.body %></p>

<p>タグ：
  <% @outfit.tags.each do |tag| %>
    <span><%= tag.name %></span>
  <% end %>
</p>

<p>投稿者：<%= link_to @outfit.user.nickname, admin_user_path(@outfit.user.id) %></p>
<p>投稿日：<%= @outfit.created_at.strftime('%Y/%m/%d') %></p>


<div>
  <%= link_to "投稿を削除する", admin_outfit_path(@outfit), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-sm btn-danger p-0 p-1", style: "font-size: 8px;" %>
</div>

<!--コメント一覧の部分テンプレート-->
<div id="comments-area">
  <%= render "admin/outfit_comments/index", outfit: @outfit, outfit_comments: @outfit_comments %>
</div>

<div>
  <p>コメント件数：<%= @outfit.outfit_comments.count %></p>
</div>
